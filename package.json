{
  "name": "katanakit-wp",
  "version": "0.1.4",
  "description": "KatanaWP — code-first WordPress configuration. Bun scripts that wrap PHP via Docker (no local PHP required).",
  "license": "MIT",
  "author": "senseikatana - Sergio Jurado",
  "repository": {
    "type": "git",
    "url": "git@github.com:senseikatana/katanakit-wp.git"
  },
  "files": [
    "src",
    "style.css",
    "index.php",
    "functions.php",
    "katana.json",
    "composer.json"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "composer": "docker run --rm -v \"$PWD\":/app -w /app composer:2",
    "test": "docker run --rm -v \"$PWD\":/app -w /app php:8.3-cli vendor/bin/phpunit",
    "lint": "docker run --rm -v \"$PWD\":/app -w /app php:8.3-cli vendor/bin/phpcs --standard=phpcs.xml",
    "lint:fix": "docker run --rm -v \"$PWD\":/app -w /app php:8.3-cli vendor/bin/phpcbf --standard=phpcs.xml",
    "wp:start": "bunx @wordpress/env start",
    "wp:stop": "bunx @wordpress/env stop",
    "check": "bun run lint && bun run test",
    "fix": "bun run lint:fix",
    "release": "bun run check && bun run bump:patch && npm publish --access public && git push && git push --tags && node scripts/generate-release-notes.mjs \"v$(node -p \"require('./package.json').version\")\" | gh release create \"v$(node -p \"require('./package.json').version\")\" --notes-file -",
    "release:minor": "bun run check && bun run bump:minor && npm publish --access public && git push && git push --tags && node scripts/generate-release-notes.mjs \"v$(node -p \"require('./package.json').version\")\" | gh release create \"v$(node -p \"require('./package.json').version\")\" --notes-file -",
    "release:major": "bun run check && bun run bump:major && npm publish --access public && git push && git push --tags && node scripts/generate-release-notes.mjs \"v$(node -p \"require('./package.json').version\")\" | gh release create \"v$(node -p \"require('./package.json').version\")\" --notes-file -",
    "bump:patch": "node scripts/bump-version.mjs patch",
    "bump:minor": "node scripts/bump-version.mjs minor",
    "bump:major": "node scripts/bump-version.mjs major",
    "bump:sync": "node scripts/bump-version.mjs --sync"
  }
}
